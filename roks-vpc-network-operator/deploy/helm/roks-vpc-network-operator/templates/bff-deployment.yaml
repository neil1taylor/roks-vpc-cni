{{- if .Values.bff.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-bff
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: vpc-network-bff
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: bff
spec:
  replicas: {{ .Values.bff.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: vpc-network-bff
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vpc-network-bff
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: bff
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: {{ .Release.Name }}-bff
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: bff
        image: "{{ .Values.bff.image.repository }}:{{ .Values.bff.image.tag }}"
        imagePullPolicy: {{ .Values.bff.image.pullPolicy }}
        ports:
        - name: https
          containerPort: {{ .Values.bff.port }}
          protocol: TCP
        - name: metrics
          containerPort: 8080
          protocol: TCP
        env:
        - name: BFF_LISTEN_ADDR
          value: "0.0.0.0:{{ .Values.bff.port }}"
        - name: BFF_LOG_LEVEL
          value: "{{ .Values.bff.logLevel }}"
        - name: BFF_TLS_CERT
          value: "/var/run/secrets/serving-cert/tls.crt"
        - name: BFF_TLS_KEY
          value: "/var/run/secrets/serving-cert/tls.key"
        - name: VPC_REGION
          value: "{{ .Values.vpc.region }}"
        - name: VPC_ID
          value: "{{ .Values.vpc.id }}"
        - name: VPC_RESOURCE_GROUP_ID
          value: "{{ .Values.vpc.resourceGroupID }}"
        - name: CLUSTER_ID
          value: "{{ .Values.cluster.id }}"
        - name: BFF_SECRET_NAME
          value: "{{ .Values.bff.apiKeySecretName }}"
        - name: BFF_SECRET_NAMESPACE
          value: "{{ .Release.Namespace }}"
        {{- if .Values.bff.csiMountPath }}
        - name: CSI_MOUNT_PATH
          value: "{{ .Values.bff.csiMountPath }}"
        {{- end }}
        {{- if .Values.bff.thanos.enabled }}
        - name: THANOS_URL
          value: "{{ .Values.bff.thanos.url }}"
        {{- end }}
        livenessProbe:
          httpGet:
            path: /healthz
            port: https
            scheme: HTTPS
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /readyz
            port: https
            scheme: HTTPS
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            cpu: {{ .Values.bff.resources.requests.cpu }}
            memory: {{ .Values.bff.resources.requests.memory }}
          limits:
            cpu: {{ .Values.bff.resources.limits.cpu }}
            memory: {{ .Values.bff.resources.limits.memory }}
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: api-key-secret
          mountPath: /var/run/secrets/api-key
          readOnly: true
        {{- if .Values.bff.csiMountPath }}
        - name: csi-credentials
          mountPath: {{ .Values.bff.csiMountPath }}
          readOnly: true
        {{- end }}
        - name: serving-cert
          mountPath: /var/run/secrets/serving-cert
          readOnly: true
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: serving-cert
        secret:
          secretName: {{ .Release.Name }}-bff-tls
      - name: api-key-secret
        secret:
          secretName: {{ .Values.bff.apiKeySecretName }}
      {{- if .Values.bff.csiMountPath }}
      - name: csi-credentials
        csi:
          driver: ibm-vpc-credentials.roks.ibm.com
          readOnly: true
      {{- end }}
      - name: tmp
        emptyDir: {}
{{- end }}
