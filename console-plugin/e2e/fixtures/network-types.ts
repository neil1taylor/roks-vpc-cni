import type { NetworkTypesInfo } from '../../src/api/types';

export const mockNetworkTypes: NetworkTypesInfo = {
  topologies: ['LocalNet', 'Layer2'],
  scopes: ['ClusterUserDefinedNetwork', 'UserDefinedNetwork'],
  roles: ['Primary', 'Secondary'],
  combinations: [
    {
      id: 'localnet-cudn-secondary',
      topology: 'LocalNet',
      scope: 'ClusterUserDefinedNetwork',
      role: 'Secondary',
      tier: 'recommended',
      ip_mode: 'static_reserved',
      requires_vpc: true,
      label: 'LocalNet Cluster Secondary',
      description: 'Cluster-wide secondary network backed by a VPC subnet.',
      ip_mode_description: 'VPC API reserves a static IP from the subnet when the VNI is created.',
    },
    {
      id: 'layer2-cudn-secondary',
      topology: 'Layer2',
      scope: 'ClusterUserDefinedNetwork',
      role: 'Secondary',
      tier: 'recommended',
      ip_mode: 'dhcp',
      label: 'Layer2 Cluster Secondary',
      description: 'Cluster-wide secondary L2 network with OVN DHCP.',
      ip_mode_description: "OVN's built-in DHCP server assigns IPs from the configured subnet range.",
    },
    {
      id: 'layer2-udn-secondary',
      topology: 'Layer2',
      scope: 'UserDefinedNetwork',
      role: 'Secondary',
      tier: 'advanced',
      ip_mode: 'dhcp',
      label: 'Layer2 Namespace Secondary',
      description: 'Namespace-scoped secondary L2 network with OVN DHCP.',
      ip_mode_description: "OVN's built-in DHCP server assigns IPs from the configured subnet range.",
    },
    {
      id: 'layer2-cudn-primary',
      topology: 'Layer2',
      scope: 'ClusterUserDefinedNetwork',
      role: 'Primary',
      tier: 'expert',
      ip_mode: 'dhcp',
      label: 'Layer2 Cluster Primary',
      description: 'Replaces the default pod network cluster-wide with an L2 network.',
      ip_mode_description: 'OVN assigns IPs from the configured subnet with persistent IPAM.',
    },
  ],
};
